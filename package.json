{
  "name": "pogo-grader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types src/parseCsv.test.ts && npx --yes tsx src/historyCalcy.test.ts",
    "pin:raid": "npx --yes tsx scripts/pin-raid-attackers.ts"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
